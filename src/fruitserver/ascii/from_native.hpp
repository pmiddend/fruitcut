#ifndef FRUITSERVER_ASCII_FROM_NATIVE_HPP_INCLUDED
#define FRUITSERVER_ASCII_FROM_NATIVE_HPP_INCLUDED

#include <fruitserver/ascii/string.hpp>
#include <string>

namespace fruitserver
{
namespace ascii
{
ascii::string const
from_native(
	std::string const &);
}
}

#endif
